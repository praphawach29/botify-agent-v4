# Botify V4 - System Features & Operation Guide

เอกสารนี้รวบรวมฟีเจอร์การทำงานของแชทบอท Botify ณ ปัจจุบัน (V4) และอธิบายกลไกการทำงานเบื้องหลังของแต่ละฟีเจอร์

## 🤖 1. Smart AI Sales Assistant (Agentic AI)
Botify ใช้สถาปัตยกรรม Native Function Calling (Tool Calling) แบบ Loop ซึ่งแปลว่า AI ไม่ได้แค่สร้างข้อความตอบกลับ แต่สามารถ "คิด" และ "เรียกใช้เครื่องมือ (Tools)" เพื่อหาข้อมูลหรือทำ Action ก่อนจะตอบลูกค้าได้จริง ปัจจุบัน AI มี Tools ทั้งหมด 10 ตัว นอกจากนี้ยังมีระบบ **AI Fallback Chain** (`claude` -> `openai` -> `gemini`) เพื่อสลับสับเปลี่ยนผู้ให้บริการ AI อัตโนมัติหาก API หลักล่ม ทำให้แชทบอททำงานได้อย่างเสถียรไม่มีสะดุด:

### 🟢 Core Tools (บริการลูกค้าพื้นฐาน)
1. **`search_products` (ค้นหาสินค้า):** AI สามารถค้นหาสินค้าจากชื่อ หมวดหมู่ หรือช่วงราคาได้ทันที โดยดึงข้อมูลจากตาราง `products` (Supabase) หรือหน้า `Products` (Google Sheet) ล่าสุดอัปเกรดให้สามารถส่ง **"ลิงก์ไดรเวอร์"** ให้ลูกค้าที่ซื้อปริ้นเตอร์หรืออุปกรณ์ไอทีได้ด้วย
2. **`check_stock` (เช็คสต็อก):** AI ค้นหาจำนวนสต็อกแบบ Real-time ป้องกันการขายของที่หมดแล้ว
3. **`get_product_image` (ส่งรูปสินค้า):** AI สามารถส่งรูปภาพสินค้าให้ลูกค้าดูในแชทได้โดยตรง

### 🛒 Sales & CRM Tools (การขายและดูแลลูกค้า)
4. **`take_order` (เปิดบิล/รับออเดอร์):** เมื่อลูกค้าตกลงซื้อ AI จะดึงข้อมูลและสร้างออเดอร์ลงในระบบ (สถานะ: รอชำระ)
5. **`update_customer_note` (บันทึกข้อมูลลูกค้า):** AI สามารถจดจำข้อมูลสำคัญของลูกค้า (เช่น แพ้อาหาร, ชอบสีแดง) ลงใน Customer Profile เพื่อการบริการที่ใส่ใจขึ้นในอนาคต
6. **`get_customer_history` (ดูประวัติการซื้อ):** AI สามารถเช็คย้อนหลังได้ว่าลูกค้าคนนี้เคยซื้ออะไรไปบ้าง เพื่อเสนอขายสินค้าที่เข้ากันได้ (Upsell)

### 💼 Business & Operation Tools (บริหารธุรกิจ)
7. **`calculate_shipping` (คำนวณค่าส่ง):** คำนวณค่าจัดส่งล่วงหน้าตามจังหวัดปลายทาง
8. **`get_active_promotions` (แนะนำโปรโมชั่น):** เสนอโปรโมชั่นที่กำลัง Active อยู่ในระบบเพื่อกระตุ้นการตัดสินใจ
9. **`create_quotation` (สร้างใบเสนอราคา):** AI สามารถรวบรวมรายการสินค้า คำนวณยอดรวม และสร้างเอกสารใบเสนอราคา (Quotation) ออกมาเป็นลิงก์ให้ลูกค้ากดพิมพ์/Save เป็น PDF ได้ทันที (รองรับเฉพาะ Supabase)
10. **`get_payment_slip_status` (เช็คสลิปโอนเงิน):** AI สามารถเช็คสถานะออเดอร์ว่าระบบหลังบ้าน (Webhook ตรวจสลิปอัตโนมัติ) ได้รับสลิปและปรับเป็น "ชำระแล้ว" หรือยัง

### 🆘 Support Tools
11. **`request_human_agent` (เรียกแอดมิน):** หากลูกค้าไม่พอใจ หรือต้องการคุยกับคนจริงๆ AI จะหยุดตอบและแจ้งเตือนไปยัง LINE ของเจ้าของร้านทันที

---

## 🏢 2. Multi-Tenant SaaS Architecture
Botify ไม่ได้เป็นแค่บอทของร้านเดียว แต่เป็นระบบ SaaS ที่รองรับหลายร้านค้า (Tenants) ในเซิร์ฟเวอร์เดียว
* **การโหลดข้อมูลร้านค้า:** ระบบใช้ `07____MULTI-TENANT_CLIENT_LOADING.js` โหลดข้อมูลร้านค้าจาก `shops` (Supabase) ผสมกับ `.env` แบบเก่า
* **Row Level Security (RLS):** มีการแยกร้านค้าอย่างเด็ดขาดโดยใช้ `req.auth.shopId` ที่ถอดรหัสจาก JWT ป้องกันปัญหาข้อมูลรั่วไหลข้ามร้านค้า (Cross-Tenant Data Leak)
* **Routing Map:** ใช้ `09_____ROUTING_MAPS__rebuilt_dynamically_.js` แยกแยะว่าข้อความที่เข้ามาจาก LINE ID หรือ FB Page ID นี้ เป็นของลูกค้าร้านไหน เพื่อส่ง Context ไปให้ AI คิดได้ถูกต้อง
* **Distributed State Caching:** เปลี่ยนจากการเก็บข้อมูลแบบ Local Memory ไปใช้ **Redis (`ioredis`)** ทำให้รองรับการสเกลเซิร์ฟเวอร์แบบแนวนอน (Horizontal Scaling) ได้อย่างไร้ปัญหา
* **Fallback Database:** ทุกร้านค้าสามารถเลือกใช้ Supabase (เป็นหลัก) และมีการซิงค์ข้อมูลแบบ 2-Way Backup ลง Google Sheets ได้

---

## 💬 3. Omni-Channel & Webhooks
* **LINE Official Account:** รับส่งข้อความ, รูปภาพ, สลิปผ่าน LINE Messaging API
* **Facebook Messenger:** รองรับการทำงานผ่าน Page Access Token / Page ID (มีระบบหลบเลี่ยง App Review สำหรับร้านค้าที่ทำแอปของตัวเอง)
* **Marketplace Sync:** ระบบตั้งเวลาซิงค์ออเดอร์จาก Shopee และ Lazada (ทุก 5 นาที) เพื่อดึงออเดอร์มารวมใน Dashboard

---

## 📑 4. Document Generation (ระบบเอกสาร)
ระบบรองรับการสร้างเอกสาร (ใบเสนอราคา, ใบส่งของ) ผ่าน URL `/doc/:id` ที่จะสร้างหน้า HTML สวยงามดึงข้อมูลจากตาราง `documents` พร้อมปุ่มสั่งพิมพ์เป็น PDF ได้ทันที ซึ่งเชื่อมต่อกับ AI Tool `create_quotation` อย่างไร้รอยต่อ

---

## 💳 5. SaaS Subscription & Free Trial
ระบบมีฟีเจอร์จำกัดสิทธิ์การเข้าถึงแบบ SaaS (Feature Gating) รองรับ 4 แพ็กเกจ:
1. **Free Trial (ทดลองใช้ฟรี):** สมัครใหม่ได้สิทธิ์ใช้งานฟรี 14 วัน พร้อม 500 AI Credits หากหมดอายุระบบ AI จะหยุดทำงานอัตโนมัติ
2. **Starter (เริ่มต้น):** 590 บาท/เดือน, 100 AI Credits, จำกัดออเดอร์/สินค้า, ไม่มีระบบบรอดแคสต์และ AI ตรวจสลิป
3. **Standard (มาตรฐาน):** 990 บาท/เดือน, 500 AI Credits, ได้ระบบบรอดแคสต์และ AI ตรวจสลิป (หัก 5 เครดิต/สลิป)
4. **Elite (มือโปร):** 1590 บาท/เดือน, 2000 AI Credits, ปลดล็อกทุกฟีเจอร์ รวมถึงการสร้างใบเสนอราคา (Quotation)

ระบบมี AI Guard คอยตรวจสอบเครดิต (AI Credits) ทุกครั้งที่เรียกใช้ฟังก์ชัน AI หากเครดิตหมด AI จะแจ้งเตือนให้ติดต่อแอดมินเพื่อเติมแพ็กเกจ
